extends index

block main
    #banner.page-header
        .row
            .col-lg-12 
                .panel.panel-info
                    .panel-heading                   
                        h1.panel-title Project #{project.title}                                               
                    .panel-body
                        | Description:                         
                        .well
                            |  #{project.description}
                        | LeadUser: 
                        .well.well-lg                                        
                            | #{project.leadUser.username}                
                if(req.user.id == project.leadUser.id)
                    - var id = project.id;                
                    a.btn.btn-default(href='/projects/' + id + '/addUser') AddUser                        
                    a.btn.btn-default(href='/projects/' + id + '/createTask') AddTask          
                    
        
  
          

